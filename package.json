{
  "name": "http-over-webrtc-js",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.2",
    "http-server": "^14.1.1",
    "typescript": "^5.0.0",
    "ws": "^8.18.3"
  }
}